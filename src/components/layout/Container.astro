---
import type { HTMLAttributes } from 'astro/types';

type ContainerProps = HTMLAttributes<'div'>;

const { class: className = '', ...attrs }: ContainerProps = Astro.props;
---

<div class:list={['container', className]} {...attrs}>
  <slot />
</div>
